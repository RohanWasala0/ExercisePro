from godot import exposed, Panel

@exposed
class exercise_card(Panel):

    def _ready(self):
        #self.get_node("exercise_label").text = Manager.mACTIVITIES[Manager.current_exercise]["exercise_name"]
        pass
